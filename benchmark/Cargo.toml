[package]

name = "benchmark"
version = "0.0.0"
authors = [ "David Renshaw <dwrenshaw@sandstorm.io>"  ]
build = "build.rs"

[[bin]]

name = "benchmark"
path = "benchmark.rs"


[build-dependencies.capnpc]
path = "../"

[dependencies]
rand = "0.1.2"
capnp = "0.1.4"
