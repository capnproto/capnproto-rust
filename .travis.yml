sudo: false
language: rust
rust:
  - nightly
  - beta
  - stable
script:
  - cargo build
  - cargo test
  - cargo doc
  - cargo clean
  - cargo build --features "rpc futures"
  - cargo test --features "rpc futures"
  - cargo doc --features "rpc futures"
